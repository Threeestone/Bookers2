<h1>Editing Book</h1>

<%= form_with model: @book do |f| %>

<div class='title'>
 <h4>Title</h4>
  <%= f.text_field :title %>
</div>
<div class="body">
 <h4>Body</h4>
  <%= f.text_area :body %>
</div>
<div class="submit">
  <%= f.submit 'Update Book' %>
</div>
<% end %>

<div class="choose2">
 <%= link_to "Show", book_path(@book.id) %>
 |
 <%= link_to "Back", books_path %>
</div>
