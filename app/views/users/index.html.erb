<h2>Users</h2>

 <table>
  <thead>
   <tr>
     <th>image</th>
     <th>name</th>
     <th colspan="3"></th>
   </tr>
  </thead>

  <tbody>
   <tr><%image_tag @book.user.get_profile_image(100,100) %></tr>
   <tr>user name: <%= @book.user.name %></tr>
   <tr><%= link_to "show", book_path(book.id) %></tr>
  </tbody>
 </table>